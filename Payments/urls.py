# from .views import register_view, user_login, user_logout
from django.urls import path
 
app_name = 'Payments'

# urlpatterns = [
#     # path('register/', views.register_view, name='register'),
#     # path('payments-dashbord', user_login, name='payments-board'),
#     ]